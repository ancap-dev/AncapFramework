name: AncapPlugin
description: "Runtime part of AncapFramework"
authors: [ PukPukov ]
website: framework.ancap-eco.ru

depend:
  - packetevents

main: ru.ancap.framework.plugin.AncapLibrary

commands:
  language:
    aliases:
      - lang

version: '${project.version}'
prefix: AncapPlugin
api-version: 1.16
load: POSTWORLD