/**
 * Unified API for sending messages, titles, sound ets to players, not hard-depend
 * on Bukkit but on primitive data. Very bad code, but it works and quit convenient
 * to use with bundle of factories and builders.
 */
